# 2026-04-17 — Project kickoff

## What I did

- Chose project name: **Fathom**. Means both a unit of depth and "to understand fully" — captures the intent.
- Decided on public GitHub visibility from day one (accountability, SEO, build-in-public as a genre).
- Decided on a single monorepo so notes, code, blog, and content can cross-link freely.
- Scaffolded the repo at `/Users/mansoor/fathom`.
- Cloned Bart Pleiter's FPGC to `/Users/mansoor/references/FPGC/` as inspiration and reference (sibling to Fathom, not inside it — strict "our work vs. their work" boundary).
- Wrote stubs for README, CONSTITUTION, ROADMAP.

## Decisions

- Reference code (FPGC and any future external projects) lives in a sibling directory `/Users/mansoor/references/`, not inside Fathom. Keeps the public repo entirely original.
- Renamed the internal `references/` folder to `library/` to avoid naming collision with the sibling directory. `library/` is for our collected PDFs, datasheets, and papers.
- Inspiration rule: *read to understand, write from scratch.* Never copy code from FPGC into Fathom, even small snippets, without checking license and documenting attribution.

## What's next

- Draft `CONSTITUTION.md` collaboratively, in order:
  1. Learning system
  2. Organization system
  3. AI-in-the-loop rules
  4. Content system
- No learning work, no code, no Anki decks, no blog posts until the constitution is drafted. This is a deliberate forcing function.

## Notes

- Checked FPGC's LICENSE: MIT. Legal to copy with attribution, but internal rule of "read to understand, write from scratch" stands for learning-ownership reasons.

## GitHub remote, identity, and LICENSE

- Set git global identity: `Mansoor` / `1955mars@gmail.com`.
- Added MIT LICENSE to the repo (matches FPGC, minimal friction for study and fork). Content licensing (blog posts, media) to be revisited when §4 is drafted.
- Created public GitHub repo: https://github.com/1955mars/fathom
- Pushed `main`; tracking `origin/main`.

## CONSTITUTION §1 drafted

Drafted the Learning system section of CONSTITUTION.md. Key decisions locked:

- **Anchor:** Feynman technique is the central learning mechanism.
- **Audience:** past self — EE/CS literate, but no topic knowledge. Expects rigor, motivation before mechanism, no unexplained jargon.
- **Concept granularity:** anything whiteboard-explainable in 2–10 minutes. Smaller folds into parent; larger splits into sub-concepts.
- **Per-concept artifacts:** written markdown doc in `notes/<source>/<concept>.md` + private unedited spoken recording in `content/videos/private/` (gitignored).
- **Public content** (blog posts, edited videos) is a separate, selective layer — not required for every concept.
- **Success criterion:** all three of (1) cold write, (2) fluid single-take speak, (3) next-day re-read still clear.
- **Scope:** every concept. No "feels simple, skip it."
- **Gate:** hard — no new chapter/module until current batch passes.
- **Gap handling:** hollow discoveries logged in `LOG/` for visibility over time.
- **Formalism:** allowed and often required (timing diagrams, waveforms, code), but intuition must come first.

Still pending: §2 Organization, §3 AI-in-the-loop, §4 Content.

## CONSTITUTION §2 drafted

Drafted Organization system. Key rules codified:

- **Tool:** Obsidian as lens; markdown remains the source of truth.
- **Vault:** whole Fathom repo is a single vault, `.obsidian/` at root.
- **Folder roles:** `notes/<source>/` holds source-bound notes (chapter summaries, exercises, running questions); `notes/concepts/` holds the canonical Feynman docs. Concepts graduate from source to concepts via `git mv`.
- **Filenames:** kebab-case slugs; first line is `# Pretty Title`.
- **Required cross-links:** concept → source + prerequisites + enables; LOG → concepts + commits touched that day; blog → concept docs it builds on.
- **Plugin whitelist:** Dataview, Excalidraw, Templater. Amendment required to add others.
- **`.obsidian/` policy:** commit only `community-plugins.json`; ignore the rest.
- **Tag taxonomy:** phase / domain / tool / status. New categories require an amendment; within categories, additions free.
- **Backup:** GitHub daily + Time Machine. No iCloud/Dropbox/OneDrive.
- **Forbidden patterns:** silent renames, orphan docs, committing private recordings, adding Obsidian plugins outside whitelist.

Supporting artifacts created this session:

- `.obsidian/community-plugins.json` — the plugin whitelist
- `notes/INDEX.md` — hand-curated map stub with Dataview queries ready for later
- `SETUP.md` — one-time setup instructions at repo root
- `.gitignore` extended with Obsidian rules and `content/videos/private/`

Still pending: §3 AI-in-the-loop, §4 Content.

## CONSTITUTION §3 drafted

Drafted the AI-in-the-loop rules — the section that most directly protects this project's core purpose. Summary:

- **Role:** AI is a tutor, reviewer, and editor. Never an author, designer, or answer-vending machine.
- **Permitted tools:** conversational AI (Claude, ChatGPT, Gemini, etc.) for explanation, critique, quizzing, review.
- **Forbidden tools:** inline code completion (Copilot, Cursor Tab, etc.) in `code/`. Agentic coding tools disallowed in `code/`, `hardware/`, `notes/concepts/` without amendment.
- **Allowed uses:** explain after I state my understanding, gap-check Feynman docs I've drafted, quiz me, review code I've written, polish finished blog drafts, summarize external references, translate jargon, devil's-advocate design decisions.
- **Forbidden uses:** writing any code, first drafts of Feynman docs, architectural design decisions, first drafts of public content, solving exercises before I've attempted them, answering conceptual questions cold.
- **State-first rule:** before any understanding question, state (1) current belief, (2) where stuck, (3) what already consulted.
- **Disclosure:** standing AI policy page on the blog + per-post disclosure line on every published piece.
- **Archival:** `ai/prompts/` for reusable templates; `ai/transcripts/` for decision-shaping conversations; ephemeral use not archived.
- **Amendment asymmetry:** loosening §3 requires a written LOG rationale + 7-day cooling-off. Tightening is free.

Artifacts created:

- `CLAUDE.md` at repo root — machine-readable §3 for AI coding agents
- `ai/README.md` updated with workspace overview
- `ai/prompts/` and `ai/transcripts/` folders with placeholders

Still pending: §4 Content.

## CONSTITUTION §4 drafted — constitution complete

Drafted the final section: Content system. Key rules codified:

- **Principle:** content is derivative of a Feynman doc that already exists — content is never a first draft. This keeps publishing downstream of learning, not competing with it.
- **Channels:** GitHub README (always-visible) + long-form blog primary; YouTube secondary (added when recording setup is good enough); short-form free-form; aggregators on major milestones only. Newsletter deferred.
- **Cadence:** milestone-driven, not scheduled. Fixed schedules force filler; filler hollows reputation faster than silence.
- **Hosting:** in-repo static site. Quartz as default (Obsidian-native — wikilinks and graph render on web); Astro as fallback. Deploy to Cloudflare Pages or GitHub Pages.
- **Brand:** real name "Mansoor"; project-branded posts ("from Fathom"); technical-personal peer-to-peer voice. Typography-only visual identity at start.
- **Draft pipeline:** `content/blog/drafts/` → `content/blog/published/` via `git mv`. Next-day re-read before publish. AI review allowed per §3.3; AI first drafts forbidden per §3.4.
- **Disclosure:** AI disclosure line + primary-sources links + corrections section on every post.
- **First post:** "Why Fathom" — published before Phase 0 begins.
- **Infrastructure timing:** blog setup is a precondition of Phase 0, not part of it.

## Constitution complete

All four sections drafted and committed:

- **§1 Learning system** — Feynman-centered (past-self audience; written + private-spoken artifact; next-day re-read gate; hard gate on progress).
- **§2 Organization system** — Obsidian as lens; notes/<source>/ vs notes/concepts/; kebab-case slugs; required cross-links; plugin whitelist; tag taxonomy; GitHub + Time Machine backup.
- **§3 AI-in-the-loop** — tutor/reviewer/editor only; state-first rule; forbidden-uses list; per-post disclosure; archival in `ai/`; CLAUDE.md enforcement for AI agents.
- **§4 Content system** — milestone-driven publishing; derivative-of-Feynman; in-repo static site; first post "Why Fathom" gates Phase 0.

## Folder restructure this session

- `content/blog/` split into `drafts/` and `published/`
- `content/videos/` split into `public/` (committed) and `private/` (gitignored per §1.4)

## Remaining preconditions before Phase 0

1. **Obsidian installed locally.** Open the Fathom folder as a vault; install the three whitelisted plugins (Dataview, Excalidraw, Templater).
2. **Time Machine enabled.** Local-backup safety net per §2.11.
3. **Dev hardware ordered.** Arty A7-100T is the working recommendation, to be reviewed before ordering.

Phase 0 begins once all preconditions are complete.

## §4 amended — publishing deferred

Reconsidered after the constitution was drafted: the blog infrastructure and first post should not be gates on Phase 0.

**Rationale:**

- Getting good at the learning process first (the Feynman workflow, concept graduation, the §1.7 gate) matters more than having an audience for it.
- Blog infrastructure setup is itself a rabbit hole that can eat weeks before any learning happens.
- Public pressure during the earliest concepts could make content cadence compete with learning cadence — exactly what §4.1 was written to prevent.
- Content is *derivative* of Feynman docs by principle (§4.1); publishing before there are concepts worth drawing from is premature.
- The public LOG still communicates progress honestly from day one, so "silent until polished" isn't really silent.

**Changes:**

- §4.8 "First post" — publishes *when ready to start sharing*, not before Phase 0. "Ready" criteria defined in the revised clause.
- §4.9 "Infrastructure setup timing" — configured *when deciding to publish*, not as a Phase 0 precondition.
- §4.1's core principle (content is always derivative of an existing Feynman doc) is untouched.

Per §4.10, §4 amendments require no cooling-off period.

## Obsidian installed + plugins pre-staged

- Installed Obsidian 1.12.7 via Homebrew cask (`brew install --cask obsidian`). App lives at `/Applications/Obsidian.app`. CLI symlink at `/opt/homebrew/bin/obsidian`.
- Pre-downloaded the three whitelisted plugins (main.js + manifest.json + styles.css each) into `.obsidian/plugins/`:
  - `dataview/` (Dataview, blacksmithgu/obsidian-dataview)
  - `obsidian-excalidraw-plugin/` (Excalidraw, zsviczian/obsidian-excalidraw-plugin)
  - `templater-obsidian/` (Templater, SilentVoid13/Templater)
- These are gitignored per §2.7 — local-only. `community-plugins.json` (committed) declares the whitelist and is already in the repo.

Remaining user-side action for precondition 1: open Obsidian → "Open folder as vault" → select `/Users/mansoor/fathom` → disable Restricted Mode → enable the three pre-staged plugins. No downloads needed inside the app.

## Precondition 1 complete

Opened the Fathom vault in Obsidian. All three whitelisted plugins (Dataview, Excalidraw, Templater) are loaded and working — the Dataview query blocks in `notes/INDEX.md` render empty tables (correct behavior, no concept docs exist yet).

## Precondition 2 — Time Machine: blocked on external drive

Checked `tmutil`: no destination configured. Using this Mac as its own TM target is not viable (Apple refuses, and same-disk backup gives no real protection).

User is ordering a USB-C SSD. Follow-up scheduled via macOS Reminders (initial attempt via `osascript` blocked by TCC/Automation permission — user will set reminder manually).

Precondition 2 stays open.

## Precondition 3 — dev board decision: Arty A7-100T

Decision captured in [`hardware/dev-board-decision.md`](../hardware/dev-board-decision.md). Point-in-time research comparing four candidates (Arty A7-100T, ULX3S-85F, DE10-Nano, Tang Mega 138K Pro Dock) is in [`hardware/dev-board-research-2026-04-17.md`](../hardware/dev-board-research-2026-04-17.md).

Key tradeoff accepted: DDR3L on Arty A7-100T vs SDRAM on FPGC's reference board. Our DDR3 controller will not be a port from Bart's SDRAM one — deeper learning, aligned with §1.

No accessories at initial order. Digilent Pmod HDMI (~$25) deferred to Phase 4.

Pending user action: place the order. Close out precondition 3 once the order is placed (not once it arrives — arrival tracking doesn't belong on the gate).

## Precondition 3 complete

Order placed at Digilent direct. Order number **203866**. Total **$328.91** ($299 item + $29.91 tax, free shipping). Arty A7-100T single unit. Free USB A-to-Micro-B cable included. Board status: awaiting shipment. Decision doc updated with order details; research doc pricing corrected from $249 to $299 (sources were stale).

## Preconditions status after today

- **1. Obsidian:** ✓ complete
- **2. Time Machine:** pending external SSD arrival
- **3. Dev hardware:** ✓ complete (order placed — arrival does not gate)

Phase 0 begins once precondition 2 closes out (SSD arrives, Time Machine configured to it).

## CONSTITUTION §3.10 added — session and context hygiene

Added §3.10 covering how to work sustainably with the AI across a multi-year project. Core rules:

- **One session per meaningful unit** (per chapter, per concept, per milestone — not per day or per phase). Marathons dilute attention.
- **Every session ends with a LOG entry** that captures the handoff — decisions, open questions, what to pick up next. Cold-start-recoverable.
- **Don't load the whole repo into context** even if it fits. Limit in-context material to what's actively in play.
- **Subagents (Agent tool) for research and heavy reads.** Their summary returns; bulk stays out.
- **Memory files stay indexical,** not knowledge dumps. Warning sign past ~100 lines.
- **Prefer fresh session over `/compact`** when the current one is heavy.
- **Setup sessions are an exception** — multi-topic arcs like today's are allowed when topics interlock and the work isn't learning.

CLAUDE.md updated with the same guidance in machine-readable form, pointing to §3.10.

Today's session is tagged as the final setup exception. Phase 0 work will follow the discipline.

## §2.11 amended — Time Machine deferred to day 30 of Phase 0

Reconsidered the SSD purchase. Buying hardware before the project has proven consistency is premature. If Fathom dies in the first month, the SSD is wasted.

**Change:** §2.11 now requires Time Machine on external SSD *within 30 days of Phase 0 start*, not before. Day 30 is measured from the first LOG entry tagged `#phase-0-start`. Pre-Phase-0 setup days don't count.

**Consequences:**
- Precondition 2 (Time Machine before Phase 0) is dropped from the gating list.
- All remaining preconditions are complete — Phase 0 is no longer gated by hardware.
- USB-C SSD order deferred; revisit at day 30.
- The 2026-04-19 reminder (if user set it) can be cancelled or repurposed for something else.

**Memory hygiene note:** with §2.11 amended and §3.10 added, project memory at `~/.claude/projects/-Users-mansoor/memory/project_fpga_computer.md` was also trimmed (85 → 72 lines total across all memory files). Governance section now points at CONSTITUTION.md instead of restating it. First application of §3.10 in practice.

## Preconditions — final state

- **1. Obsidian:** ✓ complete
- **2. Time Machine:** deferred to day 30 of Phase 0 (not a precondition anymore)
- **3. Dev hardware:** ✓ complete (order #203866)

Phase 0 is now unblocked. Next session can begin it.
